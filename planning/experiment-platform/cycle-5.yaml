project: experiment-platform
cycle: cycle-5
tasks:
- plan_key: GT-506
  title: Assign pilot treatments
  type: implement
  project: experiment-platform
  cycle: cycle-5
  priority: urgent
  estimate: 3
  labels:
  - agent-ready
  - analytics
  execution:
    executor: warp-agent
    agent_template: engineering
    parallel_group: c5-launch
    reviewer: Danny
    review_required: full
  depends_on:
  - GT-501
  - DR-412
  - GT-505
  objective: Apply the frozen matrix to companies and artifacts.
  context: Every result must map to a known hypothesis and treatment.
  inputs:
  - Pilot plan
  - 25 companies
  - Reports and email variants
  outputs:
  - Treatment assignments persisted in experiment database.
  requirements:
  - Balance allocation as designed
  - Prevent accidental reassignment
  - Record rationale for exceptions
  acceptance_criteria:
  - Every company has one valid assignment
  - Counts match plan
  validation:
  - Run allocation audit
  artifacts:
  - The required output described above
  - Tests or validation evidence
  - A completion report tied to every acceptance criterion
  - Updated documentation where behavior, schema, prompts, or operations change
  constraints:
  - Keep scope limited to this task; do not refactor unrelated components.
  - Report blockers rather than inventing missing interfaces, credentials, data, or business rules.
  - Preserve backward compatibility unless this task explicitly requires a breaking change.
  - Do not mark complete while any acceptance criterion is unmet.
  warp:
    completion_report:
    - State complete, blocked, or partially complete.
    - Summarize files, schemas, prompts, configurations, records, or workflows changed.
    - List tests and validation commands with results.
    - Map evidence to every acceptance criterion.
    - Report assumptions, unresolved risks, manual steps, and downstream tasks unblocked.
    - Include artifact paths, commit SHA, run IDs, or query results where relevant.
- plan_key: GT-511
  title: Compute pilot experiment results
  type: implement
  project: experiment-platform
  cycle: cycle-5
  priority: urgent
  estimate: 5
  labels:
  - agent-ready
  - analytics
  execution:
    executor: warp-agent
    agent_template: engineering
    parallel_group: c5-review
    reviewer: Danny
    review_required: full
  depends_on:
  - GT-508
  - GT-509
  - GT-510
  objective: Calculate results by treatment and value proposition.
  context: Pilot decisions must use observed evidence.
  inputs:
  - Email, page, CRM and call events
  - Experiment assignments
  outputs:
  - Pilot metrics and confidence summary.
  requirements:
  - Report sample sizes
  - Avoid overclaiming statistical significance
  - Show primary and secondary metrics
  acceptance_criteria:
  - Metrics reconcile to raw events
  - Missing attribution quantified
  validation:
  - Independent query cross-check
  artifacts:
  - The required output described above
  - Tests or validation evidence
  - A completion report tied to every acceptance criterion
  - Updated documentation where behavior, schema, prompts, or operations change
  constraints:
  - Keep scope limited to this task; do not refactor unrelated components.
  - Report blockers rather than inventing missing interfaces, credentials, data, or business rules.
  - Preserve backward compatibility unless this task explicitly requires a breaking change.
  - Do not mark complete while any acceptance criterion is unmet.
  warp:
    completion_report:
    - State complete, blocked, or partially complete.
    - Summarize files, schemas, prompts, configurations, records, or workflows changed.
    - List tests and validation commands with results.
    - Map evidence to every acceptance criterion.
    - Report assumptions, unresolved risks, manual steps, and downstream tasks unblocked.
    - Include artifact paths, commit SHA, run IDs, or query results where relevant.
