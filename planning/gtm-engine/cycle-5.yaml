project: gtm-engine
cycle: cycle-5
tasks:
- plan_key: GT-501
  title: Select 25 pilot companies
  type: deploy
  project: gtm-engine
  cycle: cycle-5
  priority: urgent
  estimate: 3
  labels:
  - agent-ready
  - gtm
  execution:
    executor: warp-agent
    agent_template: deploy
    wave: 1
    parallel_group: c5-targeting
    reviewer: Danny
    review_required: full
  depends_on:
  - DR-412
  objective: Choose the controlled-launch cohort.
  context: The cohort should be bounded and useful for learning.
  inputs:
  - Pilot design
  - FDA company corpus
  - ICP criteria
  outputs:
  - List of 25 companies with selection rationale and strata.
  requirements:
  - Avoid strategic accounts unless intentionally included
  - Balance relevant categories
  - Record exclusions
  acceptance_criteria:
  - Exactly 25 eligible companies
  - Strata match pilot plan
  validation:
  - Manual review by Danny
  artifacts:
  - The required output described above
  - Tests or validation evidence
  - A completion report tied to every acceptance criterion
  - Updated documentation where behavior, schema, prompts, or operations change
  constraints:
  - Keep scope limited to this task; do not refactor unrelated components.
  - Report blockers rather than inventing missing interfaces, credentials, data, or business rules.
  - Preserve backward compatibility unless this task explicitly requires a breaking change.
  - Do not mark complete while any acceptance criterion is unmet.
  warp:
    completion_report:
    - State complete, blocked, or partially complete.
    - Summarize files, schemas, prompts, configurations, records, or workflows changed.
    - List tests and validation commands with results.
    - Map evidence to every acceptance criterion.
    - Report assumptions, unresolved risks, manual steps, and downstream tasks unblocked.
    - Include artifact paths, commit SHA, run IDs, or query results where relevant.
- plan_key: GT-502
  title: Enrich CEO contacts
  type: implement
  project: gtm-engine
  cycle: cycle-5
  priority: urgent
  estimate: 5
  labels:
  - agent-ready
  - gtm
  execution:
    executor: warp-agent
    agent_template: engineering
    wave: 2
    parallel_group: c5-targeting
    reviewer: Danny
    review_required: full
  depends_on:
  - GT-501
  objective: Find and verify pilot decision-maker contact data.
  context: Outbound cannot launch without reliable recipients.
  inputs:
  - 25 companies
  - Existing enrichment tools
  - Close
  outputs:
  - Verified CEO/contact records loaded into Close.
  requirements:
  - Record source and confidence
  - Avoid guessing addresses
  - Flag unresolved contacts
  acceptance_criteria:
  - All records loaded or explicitly blocked
  - No duplicate contacts
  validation:
  - Spot-check deliverability and company match
  artifacts:
  - The required output described above
  - Tests or validation evidence
  - A completion report tied to every acceptance criterion
  - Updated documentation where behavior, schema, prompts, or operations change
  constraints:
  - Keep scope limited to this task; do not refactor unrelated components.
  - Report blockers rather than inventing missing interfaces, credentials, data, or business rules.
  - Preserve backward compatibility unless this task explicitly requires a breaking change.
  - Do not mark complete while any acceptance criterion is unmet.
  warp:
    completion_report:
    - State complete, blocked, or partially complete.
    - Summarize files, schemas, prompts, configurations, records, or workflows changed.
    - List tests and validation commands with results.
    - Map evidence to every acceptance criterion.
    - Report assumptions, unresolved risks, manual steps, and downstream tasks unblocked.
    - Include artifact paths, commit SHA, run IDs, or query results where relevant.
  run_with:
  - GT-503
- plan_key: GT-505
  title: Publish and instrument pilot reports
  type: implement
  project: gtm-engine
  cycle: cycle-5
  priority: urgent
  estimate: 5
  labels:
  - agent-ready
  - gtm
  - publishing
  - qa
  execution:
    executor: warp-agent
    agent_template: engineering
    wave: 4
    parallel_group: c5-production
    reviewer: Danny
    review_required: full
  depends_on:
  - GT-504
  - XP-307
  objective: Make approved reports available at stable tracked URLs.
  context: Email and CTA attribution depend on correct publication.
  inputs:
  - Approved reports
  - HTML renderer
  - Instrumentation
  outputs:
  - 25 live report pages with verified tracking.
  requirements:
  - Use canonical URLs
  - Verify CTA and attribution tokens
  - Apply index/noindex according to plan
  acceptance_criteria:
  - All pages load
  - Events fire
  - No draft content exposed
  validation:
  - Open and test every page
  artifacts:
  - The required output described above
  - Tests or validation evidence
  - A completion report tied to every acceptance criterion
  - Updated documentation where behavior, schema, prompts, or operations change
  constraints:
  - Keep scope limited to this task; do not refactor unrelated components.
  - Report blockers rather than inventing missing interfaces, credentials, data, or business rules.
  - Preserve backward compatibility unless this task explicitly requires a breaking change.
  - Do not mark complete while any acceptance criterion is unmet.
  warp:
    completion_report:
    - State complete, blocked, or partially complete.
    - Summarize files, schemas, prompts, configurations, records, or workflows changed.
    - List tests and validation commands with results.
    - Map evidence to every acceptance criterion.
    - Report assumptions, unresolved risks, manual steps, and downstream tasks unblocked.
    - Include artifact paths, commit SHA, run IDs, or query results where relevant.
- plan_key: GT-507
  title: Build pilot email sequence in Close
  type: implement
  project: gtm-engine
  cycle: cycle-5
  priority: urgent
  estimate: 5
  labels:
  - agent-ready
  - gtm
  execution:
    executor: warp-agent
    agent_template: engineering
    wave: 6
    parallel_group: c5-launch
    reviewer: Danny
    review_required: full
  depends_on:
  - GT-502
  - GT-506
  objective: Configure the controlled outbound sequence.
  context: The sequence must preserve experiment metadata.
  inputs:
  - Verified contacts
  - Assigned treatments
  - Email variants
  outputs:
  - Ready-to-send Close sequence and campaign.
  requirements:
  - Attach report URL and IDs
  - Keep cadence fixed except tested variable
  - Include opt-out/compliance settings
  acceptance_criteria:
  - All 25 contacts staged correctly
  - Variant mapping verified
  validation:
  - Send internal test emails
  artifacts:
  - The required output described above
  - Tests or validation evidence
  - A completion report tied to every acceptance criterion
  - Updated documentation where behavior, schema, prompts, or operations change
  constraints:
  - Keep scope limited to this task; do not refactor unrelated components.
  - Report blockers rather than inventing missing interfaces, credentials, data, or business rules.
  - Preserve backward compatibility unless this task explicitly requires a breaking change.
  - Do not mark complete while any acceptance criterion is unmet.
  warp:
    completion_report:
    - State complete, blocked, or partially complete.
    - Summarize files, schemas, prompts, configurations, records, or workflows changed.
    - List tests and validation commands with results.
    - Map evidence to every acceptance criterion.
    - Report assumptions, unresolved risks, manual steps, and downstream tasks unblocked.
    - Include artifact paths, commit SHA, run IDs, or query results where relevant.
- plan_key: GT-508
  title: Launch pilot outreach
  type: deploy
  project: gtm-engine
  cycle: cycle-5
  priority: urgent
  estimate: 3
  labels:
  - gtm
  - human
  execution:
    executor: warp-agent
    agent_template: deploy
    wave: 7
    parallel_group: c5-launch
    reviewer: Danny
    review_required: full
  depends_on:
  - GT-507
  - XP-310
  objective: Send the controlled campaign.
  context: This is the first live market test.
  inputs:
  - Approved sequence
  - Published reports
  - Attribution
  outputs:
  - 25 campaign sends with complete metadata.
  requirements:
  - Do not alter treatment after send
  - Monitor delivery failures
  - Log manual interventions
  acceptance_criteria:
  - All intended sends accounted for
  - No untracked send
  - Failures classified
  validation:
  - Reconcile Close and event data
  artifacts:
  - The required output described above
  - Tests or validation evidence
  - A completion report tied to every acceptance criterion
  - Updated documentation where behavior, schema, prompts, or operations change
  constraints:
  - Keep scope limited to this task; do not refactor unrelated components.
  - Report blockers rather than inventing missing interfaces, credentials, data, or business rules.
  - Preserve backward compatibility unless this task explicitly requires a breaking change.
  - Do not mark complete while any acceptance criterion is unmet.
  warp:
    completion_report:
    - State complete, blocked, or partially complete.
    - Summarize files, schemas, prompts, configurations, records, or workflows changed.
    - List tests and validation commands with results.
    - Map evidence to every acceptance criterion.
    - Report assumptions, unresolved risks, manual steps, and downstream tasks unblocked.
    - Include artifact paths, commit SHA, run IDs, or query results where relevant.
- plan_key: GT-509
  title: Triage replies and objections
  type: implement
  project: gtm-engine
  cycle: cycle-5
  priority: urgent
  estimate: 5
  labels:
  - agent-ready
  - gtm
  execution:
    executor: warp-agent
    agent_template: engineering
    wave: 8
    parallel_group: c5-response
    reviewer: Danny
    review_required: full
  depends_on:
  - GT-508
  objective: Capture structured market learning from every response.
  context: The CRM should record learning, not just status.
  inputs:
  - Replies
  - Close field mapping
  - Objection taxonomy
  outputs:
  - Structured classifications and next actions.
  requirements:
  - Classify positive, not now, already solved, wrong person, interesting but, unsubscribe and no response
  - Capture perceived value proposition
  acceptance_criteria:
  - Every reply classified within one business day
  - Free-text context retained
  validation:
  - Audit all replies
  artifacts:
  - The required output described above
  - Tests or validation evidence
  - A completion report tied to every acceptance criterion
  - Updated documentation where behavior, schema, prompts, or operations change
  constraints:
  - Keep scope limited to this task; do not refactor unrelated components.
  - Report blockers rather than inventing missing interfaces, credentials, data, or business rules.
  - Preserve backward compatibility unless this task explicitly requires a breaking change.
  - Do not mark complete while any acceptance criterion is unmet.
  warp:
    completion_report:
    - State complete, blocked, or partially complete.
    - Summarize files, schemas, prompts, configurations, records, or workflows changed.
    - List tests and validation commands with results.
    - Map evidence to every acceptance criterion.
    - Report assumptions, unresolved risks, manual steps, and downstream tasks unblocked.
    - Include artifact paths, commit SHA, run IDs, or query results where relevant.
- plan_key: GT-510
  title: Conduct qualification calls
  type: validate
  project: gtm-engine
  cycle: cycle-5
  priority: urgent
  estimate: 5
  labels:
  - gtm
  - human
  - validation
  execution:
    executor: warp-agent
    agent_template: validate
    wave: 9
    parallel_group: c5-response
    reviewer: Danny
    review_required: full
  depends_on:
  - GT-509
  objective: Use the report to test value and fit.
  context: Calls test which product and framing executives value.
  inputs:
  - Interested replies
  - Report
  - Qualification script
  outputs:
  - Call records with structured outcomes and next steps.
  requirements:
  - Capture disputed assumptions, urgency, role, value proposition and opportunity status
  - Do not oversell
  acceptance_criteria:
  - Every call produces structured record
  - Next action explicit
  validation:
  - Danny reviews call notes
  artifacts:
  - The required output described above
  - Tests or validation evidence
  - A completion report tied to every acceptance criterion
  - Updated documentation where behavior, schema, prompts, or operations change
  constraints:
  - Keep scope limited to this task; do not refactor unrelated components.
  - Report blockers rather than inventing missing interfaces, credentials, data, or business rules.
  - Preserve backward compatibility unless this task explicitly requires a breaking change.
  - Do not mark complete while any acceptance criterion is unmet.
  warp:
    completion_report:
    - State complete, blocked, or partially complete.
    - Summarize files, schemas, prompts, configurations, records, or workflows changed.
    - List tests and validation commands with results.
    - Map evidence to every acceptance criterion.
    - Report assumptions, unresolved risks, manual steps, and downstream tasks unblocked.
    - Include artifact paths, commit SHA, run IDs, or query results where relevant.
- plan_key: GT-512
  title: Run pilot retrospective and decisions
  type: validate
  project: gtm-engine
  cycle: cycle-5
  priority: urgent
  estimate: 3
  labels:
  - gtm
  - human
  execution:
    executor: warp-agent
    agent_template: validate
    wave: 11
    parallel_group: c5-gate
    reviewer: Danny
    review_required: full
  depends_on:
  - GT-511
  objective: Convert pilot observations into changes to the production system.
  context: The retrospective must end with promote, continue or retire decisions.
  inputs:
  - Pilot results
  - Editorial findings
  - Engineering incidents
  - Unit economics
  outputs:
  - Decision log and approved production changes.
  requirements:
  - Review business, experiments, editorial and engineering
  - Name exact prompt/CTA/treatment changes
  - Choose next bets
  acceptance_criteria:
  - Every experiment has decision
  - Every promoted change has owner/task
  - No vague 'interesting' outcome
  validation:
  - Danny approves decision log
  artifacts:
  - The required output described above
  - Tests or validation evidence
  - A completion report tied to every acceptance criterion
  - Updated documentation where behavior, schema, prompts, or operations change
  constraints:
  - Keep scope limited to this task; do not refactor unrelated components.
  - Report blockers rather than inventing missing interfaces, credentials, data, or business rules.
  - Preserve backward compatibility unless this task explicitly requires a breaking change.
  - Do not mark complete while any acceptance criterion is unmet.
  warp:
    completion_report:
    - State complete, blocked, or partially complete.
    - Summarize files, schemas, prompts, configurations, records, or workflows changed.
    - List tests and validation commands with results.
    - Map evidence to every acceptance criterion.
    - Report assumptions, unresolved risks, manual steps, and downstream tasks unblocked.
    - Include artifact paths, commit SHA, run IDs, or query results where relevant.
